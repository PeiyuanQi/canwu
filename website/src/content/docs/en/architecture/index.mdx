---
title: Architecture overview
description: Understand how Canwu organizes historical simulation with public boundaries, deterministic settlement, and inspectable evidence.
template: doc
sidebar:
  order: 1
  label: Architecture overview
hero:
  tagline: "The complete path from client to authoritative runtime: a narrow public API, deterministic settlement, causal events, and replayable randomness."
---

import { CardGrid, LinkCard, Steps } from "@astrojs/starlight/components";
import MermaidDiagram from "../../../../components/MermaidDiagram.astro";

<nav class="architecture-links" aria-label="Architecture overview navigation">
  <span class="architecture-links-label">Continue exploring:</span>
  <a href="/en/architecture/settlement/">Read settlement</a>
  <span class="architecture-links-separator" aria-hidden="true">·</span>
  <a href="/en/architecture/events/">Explore the event system</a>
  <span class="architecture-links-separator" aria-hidden="true">·</span>
  <a href="/en/architecture/model-ownership/">Review model ownership</a>
  <span class="architecture-links-separator" aria-hidden="true">·</span>
  <a href="/architecture/">中文架构</a>
</nav>

Canwu is a headless historical simulation core. A client may be a game, research tool, debug UI, or agent, but every client reaches the simulation through the same public boundaries instead of receiving a mutable reference to the live world.

## From client to kernel

| Layer | Responsibility | Public contract |
| --- | --- | --- |
| Client | Maps, rendering, input, research workflows, and agent loops | Choose the interface freely; do not own authoritative state |
| `canwu-api` | Programmatic API, queries, semantic actions, events, and snapshots | Work through validated commands and queries |
| `canwu-sim` | Scheduling, plugins, settlement, persistence, and replay | The only owner of live mutable simulation state |
| Foundation crates | IDs, time, events, world, and actor knowledge | Serializable, portable building blocks |

## Five architectural boundaries

1. **Command boundary:** external changes enter through validated commands, semantic actions, or canonical ingress; clients never rewrite the world directly.
2. **Read boundary:** the programmatic API returns detached snapshots, while the semantic API builds observations from an actor's knowledge.
3. **Determinism boundary:** time, queues, plugin order, allocation, and random streams all have explicit canonical ordering.
4. **Plugin boundary:** systems declare reads, writes, phases, resources, and random streams before the kernel validates and runs them.
5. **Evidence boundary:** commands, events, settlement, random draws, state changes, and hashes form an inspectable replay record.

## Four deep dives

<CardGrid>
  <LinkCard
    title="Settlement system"
    href="/en/architecture/settlement/"
    description="Fourteen phases, resource allocation, visibility, atomic commit, and rollback."
  />
  <LinkCard
    title="Event system"
    href="/en/architecture/events/"
    description="Event kinds, causal references, scheduling, actor knowledge, and boundary admission."
  />
  <LinkCard
    title="Random generation"
    href="/en/architecture/randomness/"
    description="Versioned streams, SplitMix64, draw evidence, ownership, and exact replay."
  />
  <LinkCard
    title="World and event model ownership"
    href="/en/architecture/model-ownership/"
    description="See which public world and event types stay, move, or retire after explicit compatibility gates."
  />
</CardGrid>

## How one action moves through the system

<Steps>
1. A client submits a command or semantic action through `canwu-api`; the kernel validates authority, preconditions, and request identity.
2. An accepted action emits a causally linked event and schedules follow-up work using representable simulation time.
3. At a settlement boundary, the kernel cuts the admission set and immutable snapshot, then runs systems in fixed phase order.
4. Systems propose reservations, state changes, domain records, and events; the kernel validates declared access and commits atomically.
5. The boundary stores events, random draws, allocations, changes, producer provenance, and hashes before clients read snapshots or actor-scoped observations.
</Steps>

## Workspace crates

| Crate | Core responsibility |
| --- | --- |
| `canwu-core` | Typed IDs, deterministic random utilities, and schema metadata |
| `canwu-time` | Historical timestamps and checked duration arithmetic |
| `canwu-decision` | Decision tickets, deterministic evaluation, controllers, and policy SDK |
| `canwu-event` | Generic event envelopes, provenance, audiences, and structured event records |
| `canwu-world` | Compatibility entities and detached snapshots for the first movement vertical slice |
| `canwu-knowledge` | Actor knowledge, source, confidence, and information age |
| `canwu-routing` | Deterministic time-dependent routing primitives |
| `canwu-transport` | Transport execution, itinerary revision, handoff, booking, and saga primitives |
| `canwu-sim` | Private runtime, scheduling, plugins, and settlement |
| `canwu-api` | Programmatic and semantic interfaces |
| `canwu-information` | Experimental authoritative information lifecycle extension |
| `canwu-society` | Experimental social diffusion simulation module |
| `canwu-technology` | Experimental evidence-based generic technology simulation extension |
| `canwu-history-research` | Optional downstream historical research assessment plugins |
| `canwu-debug` | Reference client using only the public API |

### Direct crate dependency DAG

This graph shows normal, direct dependencies declared between first-party
workspace crates in their Cargo manifests. `A → B` means **A depends on B**;
external and development dependencies are omitted.

<div class="crate-dependency-dag">
<MermaidDiagram
  label="Directed acyclic graph of direct Canwu workspace crate dependencies; arrows point from dependents to their dependencies"
  diagramLoading="Rendering crate dependency graph…"
  sourceSummary="View dependency graph source"
  sourceLoading="Dependency graph source loads with the preview."
  noScriptMessage="JavaScript is required for the dependency graph preview; the text explanation remains available above."
  definition={`flowchart TB
    subgraph ToolsExtensions["Tools and extensions"]
        Debug["canwu-debug"]
        Information["canwu-information"]
        Society["canwu-society"]
        Technology["canwu-technology"]
        History["canwu-history-research"]
    end

    subgraph Facade["Public facade"]
        Api["canwu-api"]
    end

    subgraph RuntimeMechanisms["Runtime and mechanisms"]
        Sim["canwu-sim"]
        Transport["canwu-transport"]
        Routing["canwu-routing"]
    end

    subgraph Models["Models"]
        Decision["canwu-decision"]
        Event["canwu-event"]
        Knowledge["canwu-knowledge"]
        World["canwu-world"]
    end

    subgraph Foundation["Foundation"]
        Core["canwu-core"]
        Time["canwu-time"]
    end

    Debug --> Api
    Information --> Api
    Society --> Api
    Technology --> Api
    History --> Api
    History --> Technology

    Api --> Core
    Api --> Decision
    Api --> Event
    Api --> Knowledge
    Api --> Routing
    Api --> Sim
    Api --> Time
    Api --> Transport
    Api --> World

    Sim --> Core
    Sim --> Decision
    Sim --> Event
    Sim --> Knowledge
    Sim --> Time
    Sim --> World

    Transport --> Core
    Transport --> Routing
    Transport --> Time

    Routing --> Core
    Routing --> Time
    Routing --> World

    Decision --> Core
    Decision --> Time
    Event --> Core
    Event --> Time
    Knowledge --> Core
    Knowledge --> Time
    World --> Core
    World --> Time
`}
/>
</div>

## From domain extensions to runnable kits

Canwu provides more than kernel contracts. Generic domain extensions provide
reusable mechanics, reference content packs provide replaceable domain data,
reference integrations connect those capabilities to a small world, and
starter kits compose them into vertical slices developers can run and extend.
All of these layers remain above the kernel; no historical world is embedded in
`canwu-core`.

<MermaidDiagram
  label="Architecture layers from the Canwu kernel to reference content, integrations, and host games"
  diagramLoading="Rendering diagram…"
  sourceSummary="View diagram source"
  sourceLoading="Diagram source loads with the preview."
  noScriptMessage="JavaScript is required for the diagram preview; the text-source fallback remains available above."
  definition={`flowchart TB
    Core["Simulation core"] --> Api["canwu-api"]
    Api --> Domain["Generic domain extensions"]
    Domain --> Pack["Reference content packs"]
    Domain --> Integration["Reference integrations"]
    Pack --> Integration
    Integration --> Starter["Starter kits"]
    Starter -. "copy or replace" .-> Game["CM, user games, research tools"]
    Pack -. "reuse directly" .-> Game
    Integration -. "reuse directly" .-> Game
`}
/>

Reference content packs are data, not new solvers. Reference integrations must
use the public API and be replaceable by another world or economy model. Starter
kits must demonstrate scenario creation, commands, settlement, actor-relative
reads, save/load, forks, and exact replay. The collection is expected to grow;
it is not a privileged path for one fixed demo.

## Further reading

- [Developer guide](/en/developer/)
- [Extend with plugins](/en/developer/extensions/)
- [Save, replay, and fork](/en/developer/persistence/)
- [Tutorials](/en/tutorials/)
